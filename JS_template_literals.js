let hero = "Gensk"
let level = 6
let gold = 500
let cls = "Fighter"

console.log(`The hero ${hero} is a level ${level} ${cls} and has ${gold} gold.`)
console.log(`
    ${hero}: ${cls}
    Level: ${level}
    Gold: ${gold}
`);